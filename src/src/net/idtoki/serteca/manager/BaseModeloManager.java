package net.idtoki.serteca.manager;

import java.util.logging.Logger;
import net.idtoki.serteca.model.ModeloPeer;
import net.idtoki.serteca.group.ModeloGroupBean;
import net.idtoki.serteca.model.Modelo;
import net.zylk.torque.SearchQuery;
import net.zylk.torque.BaseQuery;
import net.zylk.torque.TorqueUtils;
import net.zylk.torque.manager.BaseManager;
import org.apache.torque.TorqueException;
import java.util.Enumeration;


import net.idtoki.serteca.model.Marca;
import net.idtoki.serteca.model.MarcaPeer;

import org.apache.torque.util.Criteria;
import org.apache.torque.util.UniqueList;
import net.zylk.tools.ajax.AjaxUtils.DinamicGridBean;

/**
 * This class was autogenerated by Torque on:
 *
 * [Mon Jul 03 11:03:24 CEST 2006]
 *
 */
public abstract class BaseModeloManager extends BaseManager
{
	protected static final Logger logger = Logger.getLogger(
	   "net.idtoki.serteca.managers.BaseModeloManager");

	public static final Criteria buildSearchCriteria(DinamicGridBean dgb)
	{
		return BaseManager.buildSearchCriteria(dgb, new ModeloManager());
	}

	public static final Criteria buildSearchCriteria(String query, int offset, int limit)
	{
		return BaseManager.buildSearchCriteria(query, offset, limit, new ModeloManager());
	}

	public static final Criteria buildSearchCriteria(String query)
	{
		return BaseManager.buildSearchCriteria(query, new ModeloManager());
	}

	protected void addOrSearchCriteria(Criteria criteria,String campo,String valor, String condicion){
		Criteria.Criterion criterion = ModeloManager.getSearchCriterion(campo,valor, condicion);
		//Si existe ya un criterion hay que utilizarlo para añadir el or sino utilizará un and
		Enumeration enumeration =criteria.elements();
		if(enumeration.hasMoreElements()){
			Criteria.Criterion cr = (Criteria.Criterion) enumeration.nextElement();
			cr.or(criterion);
		}else{
			criteria.or(criterion);
		}
	}

	protected void addAndSearchCriteria(Criteria criteria,String campo, String valor, String condicion){
		Criteria.Criterion criterion = ModeloManager.getSearchCriterion(campo,valor, condicion);
		criteria.and(criterion);
	}

	protected static Criteria.Criterion getSearchCriterion(String campo,String valor, String condicion){
        Criteria criteria =  new Criteria();
        Criteria.Criterion criterion = null;

			if(campo.equals("all") || campo.equals(BaseManager.campoToLowerCase(ModeloPeer.ID)))
		{
			if(condicion.equals(BaseQuery.SingleQuery.EQUAL_CONDITION))
				criterion = TorqueUtils.addOrEqualCriteria(criterion,ModeloPeer.ID,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LIKE_CONDITION))
				criterion = TorqueUtils.addOrLikeCriteria(criterion,ModeloPeer.ID,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LESS_CONDITION))
				criterion = TorqueUtils.addOrLessCriteria(criterion,ModeloPeer.ID,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.GREATER_CONDITION))
				criterion = TorqueUtils.addOrGreaterCriteria(criterion,ModeloPeer.ID,valor,true);
		}
			if(campo.equals("all") || campo.equals(BaseManager.campoToLowerCase(ModeloPeer.NOMBRE)))
		{
			if(condicion.equals(BaseQuery.SingleQuery.EQUAL_CONDITION))
				criterion = TorqueUtils.addOrEqualCriteria(criterion,ModeloPeer.NOMBRE,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LIKE_CONDITION))
				criterion = TorqueUtils.addOrLikeCriteria(criterion,ModeloPeer.NOMBRE,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LESS_CONDITION))
				criterion = TorqueUtils.addOrLessCriteria(criterion,ModeloPeer.NOMBRE,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.GREATER_CONDITION))
				criterion = TorqueUtils.addOrGreaterCriteria(criterion,ModeloPeer.NOMBRE,valor,true);
		}
			if(campo.equals("all") || campo.equals(BaseManager.campoToLowerCase(ModeloPeer.MARCA_ID)))
		{
			if(condicion.equals(BaseQuery.SingleQuery.EQUAL_CONDITION))
				criterion = TorqueUtils.addOrEqualCriteria(criterion,ModeloPeer.MARCA_ID,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LIKE_CONDITION))
				criterion = TorqueUtils.addOrLikeCriteria(criterion,ModeloPeer.MARCA_ID,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LESS_CONDITION))
				criterion = TorqueUtils.addOrLessCriteria(criterion,ModeloPeer.MARCA_ID,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.GREATER_CONDITION))
				criterion = TorqueUtils.addOrGreaterCriteria(criterion,ModeloPeer.MARCA_ID,valor,true);
		}
									if(campo.equals("all") || campo.equals(BaseManager.campoToLowerCase(MarcaPeer.ID)))
		{
			if(condicion.equals(BaseQuery.SingleQuery.EQUAL_CONDITION))
				criterion = TorqueUtils.addOrEqualCriteria(criterion,MarcaPeer.ID,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LIKE_CONDITION))
				criterion = TorqueUtils.addOrLikeCriteria(criterion,MarcaPeer.ID,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LESS_CONDITION))
				criterion = TorqueUtils.addOrLessCriteria(criterion,MarcaPeer.ID,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.GREATER_CONDITION))
				criterion = TorqueUtils.addOrGreaterCriteria(criterion,MarcaPeer.ID,valor,true);
		}
					if(campo.equals("all") || campo.equals(BaseManager.campoToLowerCase(MarcaPeer.NOMBRE)))
		{
			if(condicion.equals(BaseQuery.SingleQuery.EQUAL_CONDITION))
				criterion = TorqueUtils.addOrEqualCriteria(criterion,MarcaPeer.NOMBRE,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LIKE_CONDITION))
				criterion = TorqueUtils.addOrLikeCriteria(criterion,MarcaPeer.NOMBRE,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LESS_CONDITION))
				criterion = TorqueUtils.addOrLessCriteria(criterion,MarcaPeer.NOMBRE,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.GREATER_CONDITION))
				criterion = TorqueUtils.addOrGreaterCriteria(criterion,MarcaPeer.NOMBRE,valor,true);
		}
						
		return criterion;
	}

	public static final ModeloGroupBean getModelos() {
    	return BaseModeloManager.getModelos(false);
	}

	public static final ModeloGroupBean getModelos(boolean paginado) {
    	return BaseModeloManager.getModelos(new Criteria(), paginado,0);
	}

	public static final ModeloGroupBean getModelos(Criteria c) {
		return BaseModeloManager.getModelos(c,false,0);
	}

	public static final ModeloGroupBean getModelos(Criteria criteria,boolean paginado, int pagina) {
		ModeloGroupBean objModeloGroupBean = new ModeloGroupBean();
		objModeloGroupBean.setPaginaActual(pagina);
		objModeloGroupBean.setModelos(criteria, paginado);
		return objModeloGroupBean;
	}

							
	public static final Modelo getModelo(int id_Id) {
		Criteria c = new Criteria();
			c.add(ModeloPeer.ID, id_Id);
			return BaseModeloManager.ModelosGroupBean2Modelo(BaseModeloManager.getModelos(c));
	}

	public static final Modelo ModelosGroupBean2Modelo(ModeloGroupBean obj){
		if(obj.size() == 1)
			return obj.getModelo(0);
		else
			return null;
	}

	private static void updateModeloAttribute(int id_Id, String valor, String campo) throws Exception
	{
		Modelo obj = BaseModeloManager.getModelo(id_Id);
			if(campo.equals(ModeloPeer.ID))
					obj.setId(Integer.parseInt(valor)); //int
					if(campo.equals(ModeloPeer.NOMBRE))
					obj.setNombre(valor); //String
					if(campo.equals(ModeloPeer.MARCA_ID))
					obj.setMarcaId(Integer.parseInt(valor)); //int
					obj.save();
	}

	public static void updateId(int id_Id, String valor) throws Exception
	{
		updateModeloAttribute(id_Id, valor, ModeloPeer.ID);
	}
	public static void updateNombre(int id_Id, String valor) throws Exception
	{
		updateModeloAttribute(id_Id, valor, ModeloPeer.NOMBRE);
	}
	public static void updateMarcaId(int id_Id, String valor) throws Exception
	{
		updateModeloAttribute(id_Id, valor, ModeloPeer.MARCA_ID);
	}

				public void addJoins(Criteria criteria)
	{
																																																	criteria.addJoin(ModeloPeer.MARCA_ID,MarcaPeer.ID);
															}
	

  public static void deleteModelo(int id_Id) throws TorqueException
  {
  	ModeloPeer.doDelete(ModeloManager.getModelo(id_Id));
  }

  public static Modelo addUpdateModelo(Modelo obj) throws TorqueException
  {
	ModeloGroupBean gb = new ModeloGroupBean();
	gb.setElemento(obj);
	gb.save();
	return obj;
  }

}


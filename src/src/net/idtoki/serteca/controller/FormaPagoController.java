package net.idtoki.serteca.controller;

import java.io.IOException;
import java.util.logging.Logger;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import net.idtoki.serteca.group.FormaPagoGroupBean;
import net.idtoki.serteca.helper.ClienteHelper;
import net.idtoki.serteca.helper.FormaPagoHelper;
import net.idtoki.serteca.manager.FormaPagoManager;
import net.zylk.tools.ajax.AjaxUtils;
import net.zylk.tools.ajax.AjaxUtils.DinamicGridBean;
import net.zylk.web.WebUtils;

import org.apache.torque.util.Criteria;

/**
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [Tue May 16 09:57:47 CEST 2006]
 *
 *  You should add additional methods to this class to meet the
 *  application requirements.  This class will only be generated as
 *  long as it does not already exist in the output directory.
 */
public class FormaPagoController
    extends net.idtoki.serteca.controller.BaseFormaPagoController
{
 private static final Logger logger = Logger.getLogger("net.idtoki.serteca.controller.FormaPagoController");

 public void init()
 {
  super.init();
  //Aqui se registran los metodos que no se quieran repetir de otros sitios.
  //por ejemplo igual quieres tener los metodos de generacion de ajax
  // en un paquete aparte y no copiarlos por aqui,  sino tenerlos disponibles
  //para todas las aplicaciones sin repetirlos en todos los sitios, pues los registras
  //y punto!!
 }
 public void serviceFormaPagoTableContent(HttpServletRequest request, HttpServletResponse response) throws IOException, ServletException
 {
  utf8RequestService(request);
  DinamicGridBean dgb = WebUtils.getDinamicGridBeanParam(request,14,7);
  Criteria c = FormaPagoManager.buildSearchCriteria(dgb);
  
  //resultado de la consulta lo guardamos en un FormaPagoGroupBean
  FormaPagoGroupBean fpgb = FormaPagoManager.getFormaPagos(c);
  dgb.setTotalSize(fpgb.getTotalSize());
  

  String[] methodos= new String[] {	
		  	FormaPagoHelper.NOMBRE_GET_METHOD_NAME+"FormaPagoParsed"
			};
  
  xmlResponseService(response,AjaxUtils.buildXmlAjaxResponseTableContentFromListObj(
		  										fpgb.getAlmacen(),
		  										methodos,
		  										FormaPagoHelper.ID_GET_METHOD_NAME, dgb));
 }
 
 public void serviceFormaPagoUlContent(HttpServletRequest request, HttpServletResponse response) throws IOException, ServletException
 {
	 utf8RequestService(request);
	 String[] methodos= new String[] {
			 FormaPagoHelper.ID_GET_METHOD_NAME,
			 FormaPagoHelper.NOMBRE_GET_METHOD_NAME+"FormaPagoParsed"
			 };

	 String param =  WebUtils.getStringParam(request, "value");
	 if(param==null || param.length() <= 0)
		 param =  WebUtils.getStringParam(request, new ClienteHelper().getFormaPagoIdName());
	 FormaPagoGroupBean fpgb = FormaPagoManager.getFormaPagos(FormaPagoManager.buildSearchCriteria(param));
	 simpleResponseService(response, AjaxUtils.buildAjaxULContentFromListObj(fpgb.getAlmacen(),methodos, FormaPagoHelper.COMPLEX_ID_GET_METHOD,"FormaPago"));
 }
}


